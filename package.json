{
  "name": "raml-typesystem-interfaces",
  "version": "0.0.11",
  "private": false,
  "scripts": {
    "compile": "rimraf -rf dist/ && ./node_modules/typescript/bin/tsc"
  },
  "main": "./dist/nominal-interfaces.js",
  "typings": "./dist/nominal-interfaces.d.ts",
  "devDependencies": {

    "typescript": "^2.0.0",
    "dev-env-installer": "0.0.5",
    "rimraf": "*"

  },
  "directories": [
    "dist"
  ],
  "dependencies": {
    "@types/node": "^6.0.55"
  }
}
